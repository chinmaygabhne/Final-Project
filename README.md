Integrated zxcvbn library to provide entropy-based password strength analysis and crack-time estimations.

Developed a CLI interface using argparse to allow users to analyze passwords or generate wordlists via the terminal.

Built a GUI application with tkinter to offer a user-friendly windowed experience for non-technical users.

Created a wordlist engine that accepts personal keywords (names, dates) to generate targeted password permutations.

Implemented leetspeak and pattern logic to automatically transform base words into common password variations like "P@$$w0rd".

Added file export functionality to save generated wordlists as .txt files compatible with industry-standard cracking tools.

Established a modular structure allowing the script to detect whether to run in command-line or graphical mode based on user input.
